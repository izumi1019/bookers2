<% @books.each do |f| %>
<%= attachment_image_tag f.user, :profile_image, size: "100x100", format: 'jpeg', fallback: "no_image.jpg" %>
  <%= link_to f.title, book_path(f.id) %>
  <%= f.body %>
<% end %>







