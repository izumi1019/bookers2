 <% if user_signed_in? %>
        <h1>Users Info</h1>
            <%= current_user.profile_image_id %>
            <%= current_user.name %>
            <%= current_user.introduction %>
            <%= link_to "編集", edit_user_path(current_user) %>
        <h1>New Book</h1>
            <%= form_with model: @book, local:true do |f| %>
                <label>title</label>
                <%= f.text_field :title %>
                <label>opinion</label>
                <%= f.text_area :body %>
                <%= f.submit "Create Book" %>
            <% end %>
            
    <% end %>
